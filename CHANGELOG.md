# Changelog

## [0.5.1] - 2026-08-10

### Changed
- 优化了一些体验。

## [0.5.0] - 2026-08-10

### Changed（确定性优化）
- **世界变化重置**：切换维度/世界/进服时重置自动收竿状态机，防止在陌生世界误抛竿。
- **配置保存缓存**：`save()` 缓存 ModConfig 引用，不再每次遍历 ConfigTracker。
- 清理未使用的 `AutoFishS.rl()` 方法。
- 审查并核对 FO 判定数学与源码一致。

## [0.4.3] - 2026-08-10

### Added
- **黑名单界面搜索框**：顶部输入框实时过滤鱼列表（按显示名或物品 ID 匹配，不区分大小写）。

## [0.4.2] - 2026-08-10

### Added
- **黑名单格子悬停显示物品信息**：像背包/JEI 一样显示鱼的名条与描述 tooltip；格子不可拿取（点击仅切换黑名单）。

## [0.4.1] - 2026-08-10

### Changed
- **黑名单界面改为物品格子网格**：每格显示一种鱼的图标，按列排列（7–18 列随宽度自适应）。点击格子切换黑名单状态，**已拉黑的格子背景变红**。列表过长支持滚轮滚动。

## [0.4.0] - 2026-08-10

### Added
- **钓鱼黑名单 GUI**：按 **F7**（可在控制设置里改绑）打开黑名单界面。
  - 列出所有鱼（`minecraft:fishes` 物品标签，含 starcatcher 全部鱼），按名称排序。
  - 每行显示 `[X] 名称`（已拉黑）/ `[  ] 名称`（未拉黑），**点击直接切换**黑名单状态并持久化。
  - 列表过长时支持滚轮滚动（仅渲染可见行）。

## [0.3.3] - 2026-08-10

### Removed
- **完全移除"自动命中开关"快捷键（F7）**：开关改为仅通过配置界面（F8）/配置文件控制。F8 仍用于打开配置界面。

## [0.3.2] - 2026-08-10

### Changed
- **F8 改为打开配置界面**（新增 `Open Config Screen` 按键，默认 F8）。原"自动命中开关"快捷键改到 **F7**。
- **配置界面进入位置整体上移**：起始 Y 从 `height/4` 改为 `max(20, height/6)`，避免首屏有内容被挤出屏幕。

### Added
- **钓鱼黑名单**（`fishingBlacklist`，针对 Starcatcher）：配置物品注册 ID 列表；当小游戏中正在钓的鱼命中黑名单时，模拟按一次 ESC 放弃该次捕获（`screen.onClose()`，等价于 ESC）。

## [0.3.1] - 2026-08-10

### Added
- **原版钓竿支持自动收竿与自动抛竿**：检测 vanilla `FishingHook` 的同步咬钩标志 `DATA_BITING`（反射解析，跨 dev/生产），咬钩自动收竿；会话结束后自动抛竿。与原版/starcatcher 钓竿并存，复用现有开关（`enableAutoReel` / `enableAutoCast`）。
- **Fishing Overhaul 模式独立配置**（`foMode`，默认 `PERFECT`）：FO 小游戏可单独选 AUTO/PERFECT，不影响 starcatcher 的 `mode`。

## [0.3.0] - 2026-08-09

### Added
- **适配 Fishing Overhaul 小游戏自动命中**（可选兼容，0 依赖）：当打开 Fishing Overhaul 的钓鱼小游戏时，自动在鱼进入可捕获区（`AUTO` 模式）或暴击区（`PERFECT` 模式）时点击。
- 采用**纯反射 + 轮询**的非侵入实现：不引用 Fishing Overhaul 任何类、无编译期依赖；未安装该模组或字段变化时优雅停用而非崩溃。

## [0.2.3] - 2026-08-09

### Fixed
- **配置界面打开时标签显示问题**：判定模式不再显示为 OFF（改为 AUTO/PERFECT），最小间隔与容差角度现在打开即显示。
- 最小间隔改为**文本输入框**形式（直接输入秒数，限制 0.1–5）。

### Changed
- **宝藏策略**：当鱼即将逃脱（进度低于 50%）时，允许命中宝藏钓点以外的钓点，避免为追宝藏而脱钩。

## [0.2.2] - 2026-08-09

### Added
- **配置界面支持鼠标滚轮滚动**：当选项过多超出屏幕时，可用滚轮滚动查看全部配置项（返回按钮固定不滚动）。

## [0.2.1] - 2026-08-09

### Added
- **Minimum hit interval**（最小命中间隔）：自动命中不再疯狂连打，两次命中之间最少等待 N 秒（默认 1s，可在 0.1–5s 调节）。
- **Treasure focus**（宝藏钓点策略）：小游戏中有宝藏钓点时，暂不命中其他钓点，专注于宝藏钓点（可开关）。
- **Negative spot handling**（负面钓点处理）：默认不点击负面钓点，可分别开启点击冰冻钓点和爆炸(TNT)钓点。

### Fixed
- **自动命中不再命中负面钓点**：修复了之前会打到 TNT 陷阱钓点（命中反而扣进度）的问题。默认跳过冰冻与爆炸钓点。

### Changed
- 版本号 0.2 → 0.2.1。
- 命中间隔配置由 tick 改为秒（`hitIntervalSeconds`）。

## [0.2] - 2026-08-09

### Added
- **Auto-Reel on Bite**（咬钩自动收杆）：检测到 starcatcher 鱼漂咬钩（同步 STATE == 3）时自动右键收杆。
- **Auto-Cast After Session**（结束后自动抛竿）：上一次钓鱼会话（鱼漂/小游戏）结束后，延迟约 1 秒自动再次抛竿。
- 以上两个新功能均可在配置界面独立开关（`enableAutoReel` / `enableAutoCast`）。
- 配置界面新增对应开关行（中英文）。

### Changed
- 版本号 0.1.0 → 0.2。

## [0.1] - 2026-08-09

### Added
- **Minigame Auto-Hit**（小游戏自动命中）：starcatcher 钓鱼小游戏中，指针移动到甜点（钓点）时自动命中。
- 模式可选：`AUTO`（甜点内即命中）/ `PERFECT`（靠近甜点中心，追求完美判定）。
- 可调参数：命中间隔（tick）、完美容差（角度）。
- 游戏内配置界面（ESC → Mods → AutoFishS → Config），纯客户端，不注册任何服务端内容、不发送自定义网络包。
- 快捷键切换（默认 F8）。
